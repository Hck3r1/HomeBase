# Optional. When unset, Expo Go derives your dev machine IP from Metro automatically.
# Set this if auto-detection fails or you use a custom port.
EXPO_PUBLIC_API_URL=http://192.168.0.115:4000/api/v1
